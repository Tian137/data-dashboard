# 2026-03-27 修改记录

## 本次目标

- 为项目补一个真正可见的登录页，而不是直接进入工作台。
- 登录页视觉参考 `careercompass` 的登录页，重点复刻卡片式布局和卡通角色眼球跟随鼠标的交互。
- 把原首页拆成登录后进入的独立工作台，并给模板页补前端门禁。
- 按用户反馈继续微调登录页、工作台入口模块和页面配色。

## 已完成

- 登录入口已改成独立页面：
  - `index.html` 现在是登录页，不再直接作为工作台首页。
  - 登录页采用卡片式两栏结构，左侧为卡通角色区，右侧为登录表单区。
- 登录页交互已接入：
  - 四个几何角色支持眼球随鼠标移动。
  - 紫色与黑色角色支持随机眨眼。
  - 聚焦邮箱输入时会触发“角色互看”动作。
  - 密码有内容且处于隐藏状态时，紫色角色会明显“转头”。
  - 切换到显示密码时，紫色角色会进入“偷看”状态。
- 登录页表单结构已按参考风格收紧：
  - 标题为 `Welcome back!`
  - 保留 `Email / Password`
  - 保留 `Remember`、`Forgot Password?`
  - 补了 `or` 分隔和 `Log in with Google`
- 当前登录是前端静态门禁：
  - 新增 `auth.js`
  - 登录状态保存在浏览器 `sessionStorage / localStorage`
  - 当前任意非空账号和密码都可进入，仅用于本地门禁和页面流程控制
- 工作台已拆分到新页面：
  - 新增 `dashboard.html`
  - 原首页的大屏数据态势、左侧指标和模板入口都迁入此页
  - 右上角补了已登录用户显示和退出按钮
- 模板录入页已补门禁：
  - `template.html` 新增 `auth.js`
  - 未登录直接访问模板页会被跳回登录页
- 工作台 `模板入口` 模块已按用户要求回退：
  - 从中途的大卡片门户样式回退到更早的简洁列表样式
- 配色调整：
  - 登录页改成更接近参考项目的浅灰卡片风格
  - 工作台首页已从深色大屏改为淡蓝渐变方向
  - 模板录入页已开始切换为淡蓝色渐变主题底色与蓝色高亮变量
- 登录页中多余的 `+` 装饰符已删除。

## 涉及文件

- `/home/maya/projects/data-dashboard/index.html`
- `/home/maya/projects/data-dashboard/dashboard.html`
- `/home/maya/projects/data-dashboard/auth.js`
- `/home/maya/projects/data-dashboard/app.css`
- `/home/maya/projects/data-dashboard/template.html`
- `/home/maya/projects/data-dashboard/WORKLOG-2026-03-27.md`

## 当前状态

- 页面入口关系已变为：
  - 登录页：`http://127.0.0.1:4173/index.html`
  - 工作台：`http://127.0.0.1:4173/dashboard.html`
  - 模板页：`http://127.0.0.1:4173/template.html?book=1|2|4`
- 登录成功后会进入工作台。
- 工作台和模板页都要求先通过前端门禁。
- 登录页和工作台都依赖最新版 `app.css` 版本参数，避免浏览器缓存旧样式。

## 已验证

- 本地静态服务下：
  - `index.html` 返回 `HTTP 200`
  - `dashboard.html` 返回 `HTTP 200`
  - `template.html?book=2` 返回 `HTTP 200`
- 登录页已通过无头浏览器 DOM 检查，确认：
  - 卡片式布局已生效
  - 角色节点已渲染
  - 眼球位移样式在页面中存在
- 工作台和模板页已确认接入 `auth.js`

## 风险与注意事项

- 当前登录不是后端鉴权，只是前端门禁。
- `Log in with Google`、`Forgot Password?`、`Sign up now` 目前都只是静态占位提示。
- 工作台首页淡蓝色渐变如果浏览器仍显示旧深色，需要执行一次 `Ctrl+F5` 强制刷新。
- 无头浏览器在当前 WSL 环境下部分后续调用存在 `UtilBindVsockAnyPort` 错误，因此登录后完整点击链路仍建议在真实浏览器中手动确认。
