# 2026-03-23 / 2026-03-24 修改记录

## 已完成

- 首页入口已收敛为 3 个：
  - `煤炭售价统计表`
  - `产量与工业总产值统计表`
  - `煤炭销售统计表`
- 旧的 `book=3` 链接已兼容映射到 `book=2`，避免旧收藏或旧地址失效。
- 模板页整体头部和版式已统一收紧：
  - 顶部 `sheet tab`、自动保存状态、返回按钮、导出/清空按钮都已重新排版。
  - 中间重复的 `Worksheet` 标题区和售价页摘要卡片已移除。
  - 表格可视区域已释放更多空间。
- `煤炭售价统计表`
  - 恢复了 `一、混煤（元/吨）`、`二、电煤（元/吨）`、`三、市场煤` 三行。
  - 仍只保留“品种”这一列作为左侧固定标题。
  - 已兼容旧本地存储，打开旧数据时会自动补齐这三行。
- `产量与工业总产值统计表`
  - 两张月度表都新增了 `年份 + 月份` 筛选模块。
  - 年份已按本地存储分桶，支持后续跨年、跨月长期录入。
  - 月份切换会在表格内部横向定位到对应月份列。
  - 左侧 `板块及公司名称`、`公司`、`产品/说明` 列宽已多轮调整。
  - 月度表当前选中月份会高亮，便于确认是否定位正确。
- `煤炭销售统计表`
  - 只保留一个周模板，不再同时展示第二套对比模板。
  - 表格主体已改成内部滚动，表头固定。
  - `原因分析` 改成工具栏按钮，点击后以右侧抽屉/侧边面板方式打开。
- 三类表格都继续支持编辑冻结标题内容，并支持基础结构调整：
  - 月度表：可改左侧行标题、固定列表头、月份列标题；可新增/删除行、月份列。
  - 售价表：可改表头、品种名；可新增/删除品种行；可新增/删除自定义列。
  - 销售表：可改销售类别、日期标题、分组标题；可新增/删除行、日期列。
- 结构调整、筛选状态和录入数据都会继续写入浏览器本地存储。

## 涉及文件

- `/home/maya/projects/data-dashboard/template.js`
- `/home/maya/projects/data-dashboard/template-data.js`
- `/home/maya/projects/data-dashboard/app.css`
- `/home/maya/projects/data-dashboard/index.html`
- `/home/maya/projects/data-dashboard/template.html`
- `/home/maya/projects/data-dashboard/README-WSL.md`

## 当前状态

- `template.js` 已通过 `node.exe --check` 语法检查。
- 本地静态服务 `http://127.0.0.1:4173` 可正常返回首页和模板页。
- 已做过多轮浏览器/无头浏览器页面渲染检查：
  - 首页入口数量和文案
  - 售价表新增三行
  - 月度表筛选模块
  - 销售表固定表头和原因分析侧边面板
- 用户已确认月度表“点击几月就对应几月”的定位逻辑最终对齐。

## 下次继续时建议先做

1. 实际在浏览器中做一次完整录入回归，尤其是月度表跨年录入、切换年份后数据是否符合预期。
2. 如果长期使用月度筛选，可把“年份”进一步做成下拉历史年份列表加“新增年份”交互。
3. 如果用户继续细化，可把“新增/删除”扩展为“在指定位置插入”。
4. 如果需要长期维护，可继续拆分 `template.js`，把月度表、售价表、销售表逻辑分模块。

## 相关会话记录

- `C:\Users\admin01\.codex\sessions\2026\03\23`
- `C:\Users\admin01\.codex\sessions\2026\03\24`
